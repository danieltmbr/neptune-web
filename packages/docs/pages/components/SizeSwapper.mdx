import { LiveEditorBlock, GeneratePropsTable } from '../../utils';
import { SizeSwapper } from '@transferwise/components';
import code from '../../liveEditorCode/SizeSwapper.code';

<LiveEditorBlock code={code} scope={{ SizeSwapper }} />
<GeneratePropsTable componentName="SizeSwapper" />

export const meta = {
  name: 'SizeSwapper',
};